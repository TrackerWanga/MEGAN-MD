// MEGAN-MD Basic Commands - With Dropdown Menus

const config = require('../../megan/config');
const moment = require('moment-timezone');
const os = require('os');

const commands = [];

const CHANNEL_LINK = 'https://whatsapp.com/channel/0029VbCWWXi9hXF2SXUHgZ1b';
const BOT_LOGO = 'https://files.catbox.moe/0v8bkv.png';

// Helper to send interactive buttons (simple reply/url/copy)
async function sendInteractive(sock, from, options, quotedMsg) {
    const { sendInteractiveMessage } = require('gifted-btns');
    
    try {
        return await sendInteractiveMessage(sock, from, {
            text: options.text,
            footer: options.footer || '> created by wanga',
            interactiveButtons: options.buttons || []
        }, { quoted: quotedMsg });
    } catch (error) {
        console.error('Interactive error:', error);
        await sock.sendMessage(from, { text: options.text }, { quoted: quotedMsg });
    }
}

// Helper to send dropdown menu (single_select)
async function sendDropdownMenu(sock, from, options, quotedMsg) {
    const { sendInteractiveMessage } = require('gifted-btns');
    
    try {
        return await sendInteractiveMessage(sock, from, {
            text: options.text,
            footer: options.footer || '> created by wanga',
            interactiveButtons: [
                {
                    name: 'single_select',
                    buttonParamsJson: JSON.stringify({
                        title: options.title || 'Select an option',
                        sections: options.sections
                    })
                }
            ]
        }, { quoted: quotedMsg });
    } catch (error) {
        console.error('Dropdown error:', error);
        await sock.sendMessage(from, { text: options.text }, { quoted: quotedMsg });
    }
}

// Helper to send buttons (simple reply buttons)
async function sendButtonMenu(sock, from, options, quotedMsg) {
    const { sendButtons } = require('gifted-btns');
    
    try {
        return await sendButtons(sock, from, {
            title: options.title || '𝐌𝐄𝐆𝐀𝐍-𝐌𝐃',
            text: options.text,
            footer: options.footer || '> created by wanga',
            image: options.image ? { url: options.image } : null,
            buttons: options.buttons || []
        }, { quoted: quotedMsg });
    } catch (error) {
        console.error('Button error:', error);
        await sock.sendMessage(from, { text: options.text }, { quoted: quotedMsg });
    }
}

// ==================== AI DROPDOWN MENU ====================
async function sendAIDropdown(sock, from, msg, buttons) {
    const sections = [
        {
            title: '🤖 AI Models',
            rows: [
                { id: `${config.PREFIX}megan`, title: 'Megan AI', description: 'Cloudflare AI' },
                { id: `${config.PREFIX}gemini`, title: 'Gemini', description: 'Google AI with vision' },
                { id: `${config.PREFIX}gpt`, title: 'GPT OSS', description: '120B model' },
                { id: `${config.PREFIX}deepseek`, title: 'DeepSeek', description: 'R1 reasoning model' },
                { id: `${config.PREFIX}mistral`, title: 'Mistral', description: 'Fast and efficient' },
                { id: `${config.PREFIX}duckai`, title: 'DuckAI', description: 'Multi-model AI' }
            ]
        },
        {
            title: '📖 Specialized AI',
            rows: [
                { id: `${config.PREFIX}codellama`, title: 'CodeLlama', description: 'Coding assistant' },
                { id: `${config.PREFIX}teacher`, title: 'Teacher AI', description: 'Educational help' },
                { id: `${config.PREFIX}bibleai`, title: 'Bible AI', description: 'Biblical Q&A' },
                { id: `${config.PREFIX}gita`, title: 'Gita AI', description: 'Bhagavad Gita' }
            ]
        },
        {
            title: '⚙️ Settings',
            rows: [
                { id: `${config.PREFIX}aimode`, title: 'AI Mode', description: 'Short/Normal/Detailed' },
                { id: `${config.PREFIX}aimenu`, title: 'Full AI Menu', description: 'All AI commands' }
            ]
        }
    ];
    
    await sendDropdownMenu(sock, from, {
        text: `🤖 *AI COMMANDS*\n\nSelect an AI model from the dropdown below.`,
        footer: '> created by wanga',
        title: '🤖 AI Models',
        sections: sections
    }, msg);
}

// ==================== AI IMAGE DROPDOWN MENU ====================
async function sendAIImageDropdown(sock, from, msg, buttons) {
    const sections = [
        {
            title: '🖼️ Image Search',
            rows: [
                { id: `${config.PREFIX}image`, title: 'Image Search', description: 'Search images' },
                { id: `${config.PREFIX}images`, title: 'Bing Images', description: 'Bing image search' },
                { id: `${config.PREFIX}pinterest`, title: 'Pinterest', description: 'Pinterest images' },
                { id: `${config.PREFIX}wallpaper`, title: 'Wallpaper', description: 'HD wallpapers' }
            ]
        },
        {
            title: '🎨 AI Generation',
            rows: [
                { id: `${config.PREFIX}imagine`, title: 'Imagine AI', description: 'Generate images' },
                { id: `${config.PREFIX}create`, title: 'Create Logo', description: 'Logo/text effects' },
                { id: `${config.PREFIX}textpro`, title: 'TextPro', description: 'Text effects' }
            ]
        },
        {
            title: '✨ Image Editing',
            rows: [
                { id: `${config.PREFIX}beautiful`, title: 'Beautiful', description: 'Add caption effect' },
                { id: `${config.PREFIX}circle`, title: 'Circle Image', description: 'Make circular' },
                { id: `${config.PREFIX}removebg`, title: 'Remove BG', description: 'Remove background' }
            ]
        }
    ];
    
    await sendDropdownMenu(sock, from, {
        text: `🎨 *AI IMAGE COMMANDS*\n\nSelect an image tool from the dropdown below.`,
        footer: '> created by wanga',
        title: '🎨 AI Image Tools',
        sections: sections
    }, msg);
}

// ==================== DOWNLOADER DROPDOWN MENU ====================
async function sendDownloaderDropdown(sock, from, msg, buttons) {
    const sections = [
        {
            title: '🎵 Music Downloaders',
            rows: [
                { id: `${config.PREFIX}play`, title: 'Play', description: 'Search and download' },
                { id: `${config.PREFIX}ytmp3`, title: 'YT to MP3', description: 'YouTube to audio' },
                { id: `${config.PREFIX}spotifydl`, title: 'Spotify', description: 'Spotify tracks' },
                { id: `${config.PREFIX}soundcloud`, title: 'SoundCloud', description: 'SoundCloud tracks' }
            ]
        },
        {
            title: '📱 Social Media',
            rows: [
                { id: `${config.PREFIX}tiktokdl`, title: 'TikTok', description: 'TikTok videos' },
                { id: `${config.PREFIX}instagramdl`, title: 'Instagram', description: 'Posts & reels' },
                { id: `${config.PREFIX}facebookdl`, title: 'Facebook', description: 'Facebook videos' },
                { id: `${config.PREFIX}twitterdl`, title: 'Twitter/X', description: 'Videos & media' }
            ]
        },
        {
            title: '🌐 Universal',
            rows: [
                { id: `${config.PREFIX}savefrom`, title: 'SaveFrom', description: 'Universal downloader' },
                { id: `${config.PREFIX}dlfiles`, title: 'DL Files', description: 'Direct file download' }
            ]
        }
    ];
    
    await sendDropdownMenu(sock, from, {
        text: `⬇️ *DOWNLOADER COMMANDS*\n\nSelect a downloader from the dropdown below.`,
        footer: '> created by wanga',
        title: '⬇️ Downloaders',
        sections: sections
    }, msg);
}

// ==================== GROUP DROPDOWN MENU ====================
async function sendGroupDropdown(sock, from, msg, buttons) {
    const sections = [
        {
            title: '👥 Member Management',
            rows: [
                { id: `${config.PREFIX}add`, title: 'Add', description: 'Add members' },
                { id: `${config.PREFIX}remove`, title: 'Remove', description: 'Remove members' },
                { id: `${config.PREFIX}promote`, title: 'Promote', description: 'Make admin' },
                { id: `${config.PREFIX}demote`, title: 'Demote', description: 'Remove admin' }
            ]
        },
        {
            title: '📢 Tagging',
            rows: [
                { id: `${config.PREFIX}tagall`, title: 'Tag All', description: 'Tag all members' },
                { id: `${config.PREFIX}tagadmins`, title: 'Tag Admins', description: 'Tag admins only' }
            ]
        },
        {
            title: '🔧 Group Settings',
            rows: [
                { id: `${config.PREFIX}invite`, title: 'Invite Link', description: 'Get invite link' },
                { id: `${config.PREFIX}groupinfo`, title: 'Group Info', description: 'Group details' },
                { id: `${config.PREFIX}lock`, title: 'Lock Messages', description: 'Admins only' }
            ]
        }
    ];
    
    await sendDropdownMenu(sock, from, {
        text: `👥 *GROUP COMMANDS*\n\nSelect a group action from the dropdown below.`,
        footer: '> created by wanga',
        title: '👥 Group Management',
        sections: sections
    }, msg);
}

// ==================== MEDIA DROPDOWN MENU ====================
async function sendMediaDropdown(sock, from, msg, buttons) {
    const sections = [
        {
            title: '🎨 Stickers',
            rows: [
                { id: `${config.PREFIX}sticker`, title: 'Sticker', description: 'Create sticker' },
                { id: `${config.PREFIX}toimage`, title: 'To Image', description: 'Sticker to image' }
            ]
        },
        {
            title: '🎵 Audio',
            rows: [
                { id: `${config.PREFIX}say`, title: 'Say', description: 'Text to speech' },
                { id: `${config.PREFIX}voice`, title: 'Voice Note', description: 'PTT voice note' },
                { id: `${config.PREFIX}toaudio`, title: 'To Audio', description: 'Video to audio' }
            ]
        },
        {
            title: '🖼️ Random',
            rows: [
                { id: `${config.PREFIX}waifu`, title: 'Waifu', description: 'Random waifu' },
                { id: `${config.PREFIX}neko`, title: 'Neko', description: 'Random neko' },
                { id: `${config.PREFIX}meme`, title: 'Meme', description: 'Random meme' }
            ]
        }
    ];
    
    await sendDropdownMenu(sock, from, {
        text: `🎵 *MEDIA COMMANDS*\n\nSelect a media tool from the dropdown below.`,
        footer: '> created by wanga',
        title: '🎵 Media Tools',
        sections: sections
    }, msg);
}

// ==================== SEARCH DROPDOWN MENU ====================
async function sendSearchDropdown(sock, from, msg, buttons) {
    const sections = [
        {
            title: '🌐 Web Search',
            rows: [
                { id: `${config.PREFIX}google`, title: 'Google', description: 'Google search' },
                { id: `${config.PREFIX}bing`, title: 'Bing', description: 'Bing search' },
                { id: `${config.PREFIX}duckduckgo`, title: 'DuckDuckGo', description: 'Private search' }
            ]
        },
        {
            title: '📚 Knowledge',
            rows: [
                { id: `${config.PREFIX}wiki`, title: 'Wikipedia', description: 'Encyclopedia' },
                { id: `${config.PREFIX}dictionary`, title: 'Dictionary', description: 'Word definitions' },
                { id: `${config.PREFIX}movie`, title: 'Movie', description: 'Movie info' },
                { id: `${config.PREFIX}anime`, title: 'Anime', description: 'Anime search' }
            ]
        },
        {
            title: '🎬 Media',
            rows: [
                { id: `${config.PREFIX}youtube`, title: 'YouTube', description: 'Search videos' },
                { id: `${config.PREFIX}weather`, title: 'Weather', description: 'Weather info' }
            ]
        }
    ];
    
    await sendDropdownMenu(sock, from, {
        text: `🔍 *SEARCH COMMANDS*\n\nSelect a search tool from the dropdown below.`,
        footer: '> created by wanga',
        title: '🔍 Search Tools',
        sections: sections
    }, msg);
}

// ==================== TOOLS DROPDOWN MENU ====================
async function sendToolsDropdown(sock, from, msg, buttons) {
    const sections = [
        {
            title: '🔢 Encoding',
            rows: [
                { id: `${config.PREFIX}binary`, title: 'Binary', description: 'Text to binary' },
                { id: `${config.PREFIX}base64`, title: 'Base64', description: 'Encode/decode' },
                { id: `${config.PREFIX}hash`, title: 'Hash', description: 'MD5, SHA1, SHA256' },
                { id: `${config.PREFIX}morse`, title: 'Morse', description: 'Morse code' }
            ]
        },
        {
            title: '🔐 Security',
            rows: [
                { id: `${config.PREFIX}encrypt`, title: 'Encrypt', description: 'AES encryption' },
                { id: `${config.PREFIX}decrypt`, title: 'Decrypt', description: 'AES decryption' },
                { id: `${config.PREFIX}password`, title: 'Password', description: 'Generate passwords' },
                { id: `${config.PREFIX}vcc`, title: 'VCC', description: 'Fake credit cards' }
            ]
        },
        {
            title: '🎲 Utilities',
            rows: [
                { id: `${config.PREFIX}calculate`, title: 'Calculate', description: 'Math calculator' },
                { id: `${config.PREFIX}fliptext`, title: 'Flip Text', description: 'Upside down text' },
                { id: `${config.PREFIX}zodiak`, title: 'Zodiak', description: 'Zodiac info' }
            ]
        }
    ];
    
    await sendDropdownMenu(sock, from, {
        text: `🛠️ *TOOLS*\n\nSelect a tool from the dropdown below.`,
        footer: '> created by wanga',
        title: '🛠️ Utility Tools',
        sections: sections
    }, msg);
}

// ==================== FEATURES DROPDOWN MENU ====================
async function sendFeaturesDropdown(sock, from, msg, buttons) {
    const sections = [
        {
            title: '⚙️ Auto Features',
            rows: [
                { id: `${config.PREFIX}autoreact`, title: 'Auto React', description: 'Auto reaction' },
                { id: `${config.PREFIX}autoread`, title: 'Auto Read', description: 'Auto mark as read' }
            ]
        },
        {
            title: '🛡️ Anti Features',
            rows: [
                { id: `${config.PREFIX}antidelete`, title: 'Anti Delete', description: 'Catch deleted messages' },
                { id: `${config.PREFIX}anticall`, title: 'Anti Call', description: 'Block calls' },
                { id: `${config.PREFIX}antilink`, title: 'Anti Link', description: 'Block links' }
            ]
        },
        {
            title: '📱 Status Features',
            rows: [
                { id: `${config.PREFIX}autoviewstatus`, title: 'Auto View Status', description: 'View statuses' },
                { id: `${config.PREFIX}autoreactstatus`, title: 'Auto React Status', description: 'React to statuses' },
                { id: `${config.PREFIX}setstatusemoji`, title: 'Status Emojis', description: 'Set reaction emojis' }
            ]
        }
    ];
    
    await sendDropdownMenu(sock, from, {
        text: `⚙️ *FEATURE TOGGLES*\n\nSelect a feature to toggle from the dropdown below.`,
        footer: '> created by wanga',
        title: '⚙️ Features',
        sections: sections
    }, msg);
}

// ==================== MAIN MENU COMMAND ====================
commands.push({
    name: 'menu',
    description: 'Show bot menu with dropdown buttons',
    aliases: ['help', 'cmds', 'allmenu', '📋 Menu'],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        
        const now = moment().tz(config.TIMEZONE || 'Africa/Nairobi');
        const timeStr = now.format('h:mm A');
        const dateStr = now.format('DD/MM/YYYY');
        
        const uptime = process.uptime();
        const days = Math.floor(uptime / 86400);
        const hours = Math.floor((uptime % 86400) / 3600);
        const minutes = Math.floor((uptime % 3600) / 60);
        
        const totalMem = os.totalmem() / (1024 * 1024 * 1024);
        const freeMem = os.freemem() / (1024 * 1024 * 1024);
        const usedMem = (totalMem - freeMem).toFixed(2);
        const totalMemFixed = totalMem.toFixed(2);

        const menuText = `*𝐌𝐄𝐆𝐀𝐍 𝐌𝐃*\n\n` +
            `✧･ﾟ: ✧･ﾟ:  𝐒𝐘𝐒𝐓𝐄𝐌  :･ﾟ✧:･ﾟ✧\n` +
            `➤ Version  : 3.0.0\n` +
            `➤ Owner    : ${config.OWNER_NAME}\n` +
            `➤ Prefix   : ${config.PREFIX}\n` +
            `➤ Commands : ${bot.commands.size}\n` +
            `➤ Uptime   : ${days}d ${hours}h ${minutes}m\n` +
            `➤ Time     : ${timeStr}\n` +
            `➤ Date     : ${dateStr}\n` +
            `➤ RAM      : ${usedMem}GB/${totalMemFixed}GB\n\n` +
            `📌 *Click a category button below to open dropdown menu*`;

        await sendButtonMenu(sock, from, {
            title: '🎯 MEGAN-MD MAIN MENU',
            text: menuText,
            footer: '> created by wanga',
            image: BOT_LOGO,
            buttons: [
                { id: 'show_ai_dropdown', text: '🤖 AI' },
                { id: 'show_ai_image_dropdown', text: '🎨 AI Image' },
                { id: 'show_downloader_dropdown', text: '⬇️ Downloader' },
                { id: 'show_group_dropdown', text: '👥 Group' },
                { id: 'show_media_dropdown', text: '🎵 Media' },
                { id: 'show_search_dropdown', text: '🔍 Search' },
                { id: 'show_tools_dropdown', text: '🛠️ Tools' },
                { id: 'show_features_dropdown', text: '⚙️ Features' },
                { name: 'cta_url', buttonParamsJson: JSON.stringify({ display_text: '📢 Channel', url: CHANNEL_LINK }) }
            ]
        }, msg);
        await react('✅');
    }
});

// ==================== DROPDOWN HANDLERS ====================
commands.push({
    name: 'show_ai_dropdown',
    description: 'Show AI dropdown menu',
    aliases: [],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        await sendAIDropdown(sock, from, msg, buttons);
    }
});

commands.push({
    name: 'show_ai_image_dropdown',
    description: 'Show AI Image dropdown menu',
    aliases: [],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        await sendAIImageDropdown(sock, from, msg, buttons);
    }
});

commands.push({
    name: 'show_downloader_dropdown',
    description: 'Show Downloader dropdown menu',
    aliases: [],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        await sendDownloaderDropdown(sock, from, msg, buttons);
    }
});

commands.push({
    name: 'show_group_dropdown',
    description: 'Show Group dropdown menu',
    aliases: [],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        await sendGroupDropdown(sock, from, msg, buttons);
    }
});

commands.push({
    name: 'show_media_dropdown',
    description: 'Show Media dropdown menu',
    aliases: [],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        await sendMediaDropdown(sock, from, msg, buttons);
    }
});

commands.push({
    name: 'show_search_dropdown',
    description: 'Show Search dropdown menu',
    aliases: [],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        await sendSearchDropdown(sock, from, msg, buttons);
    }
});

commands.push({
    name: 'show_tools_dropdown',
    description: 'Show Tools dropdown menu',
    aliases: [],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        await sendToolsDropdown(sock, from, msg, buttons);
    }
});

commands.push({
    name: 'show_features_dropdown',
    description: 'Show Features dropdown menu',
    aliases: [],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        await sendFeaturesDropdown(sock, from, msg, buttons);
    }
});

// ==================== KEEP EXISTING COMMANDS ====================

// AIMENU - also show dropdown
commands.push({
    name: 'aimenu',
    description: 'Show AI commands dropdown',
    aliases: ['aihelp', 'aimen'],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        await sendAIDropdown(sock, from, msg, buttons);
        await react('✅');
    }
});

// IMAGEMEN - also show dropdown
commands.push({
    name: 'imagemen',
    description: 'Show AI Image commands dropdown',
    aliases: ['imagehelp', 'imgmen'],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        await sendAIImageDropdown(sock, from, msg, buttons);
        await react('✅');
    }
});

// DOWNLOADHELP - also show dropdown
commands.push({
    name: 'downloadhelp',
    description: 'Show Downloader commands dropdown',
    aliases: ['dlhelp', 'playhelp'],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        await sendDownloaderDropdown(sock, from, msg, buttons);
        await react('✅');
    }
});

// GROUPHELP - also show dropdown
commands.push({
    name: 'grouphelp',
    description: 'Show Group commands dropdown',
    aliases: ['ghelp', 'groupmen'],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        await sendGroupDropdown(sock, from, msg, buttons);
        await react('✅');
    }
});

// MEDIA - also show dropdown
commands.push({
    name: 'media',
    description: 'Show Media commands dropdown',
    aliases: ['mediahelp', 'mediamen'],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        await sendMediaDropdown(sock, from, msg, buttons);
        await react('✅');
    }
});

// SEARCHHELP - also show dropdown
commands.push({
    name: 'searchhelp',
    description: 'Show Search commands dropdown',
    aliases: ['searchmen'],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        await sendSearchDropdown(sock, from, msg, buttons);
        await react('✅');
    }
});

// TOOLS - also show dropdown
commands.push({
    name: 'tools',
    description: 'Show Tools dropdown menu',
    aliases: ['toolhelp', 'toolsmen'],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        await sendToolsDropdown(sock, from, msg, buttons);
        await react('✅');
    }
});

// FEATURES - also show dropdown
commands.push({
    name: 'features',
    description: 'Show Features dropdown menu',
    aliases: ['featurehelp', 'featuremen'],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        await sendFeaturesDropdown(sock, from, msg, buttons);
        await react('✅');
    }
});

// ==================== BASIC COMMANDS ====================
commands.push({
    name: 'ping',
    description: 'Check bot response time',
    aliases: ['p'],
    async execute({ msg, from, sender, args, bot, sock, react, reply, buttons }) {
        const start = Date.now();
        await new Promise(resolve => setTimeout(resolve, 50));
        const ping = Date.now() - start;
        
        await sendButtonMenu(sock, from, {
            title: '⚡ Bot Speed',
            text: `*Pong!* 🏓\n\n📡 Response: ${ping}ms`,
            footer: '> created by wanga',
            buttons: [
                { id: `${config.PREFIX}uptime`, text: '⏱️ Uptime' },
                { id: `${config.PREFIX}menu`, text: '📋 Menu' },
                { name: 'cta_url', buttonParamsJson: JSON.stringify({ display_text: '📢 Channel', url: CHANNEL_LINK }) }
            ]
        }, msg);
        await react('✅');
    }
});

commands.push({
    name: 'uptime',
    description: 'Show bot uptime',
    aliases: ['runtime'],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        const uptime = process.uptime();
        const days = Math.floor(uptime / 86400);
        const hours = Math.floor((uptime % 86400) / 3600);
        const minutes = Math.floor((uptime % 3600) / 60);
        const seconds = Math.floor(uptime % 60);

        await sendButtonMenu(sock, from, {
            title: '⏱️ Bot Uptime',
            text: `📅 *Days:* ${days}\n⏰ *Hours:* ${hours}\n⏱️ *Minutes:* ${minutes}\n⏲️ *Seconds:* ${seconds}`,
            footer: '> created by wanga',
            buttons: [
                { id: `${config.PREFIX}ping`, text: '⚡ Ping' },
                { id: `${config.PREFIX}menu`, text: '📋 Menu' },
                { name: 'cta_url', buttonParamsJson: JSON.stringify({ display_text: '📢 Channel', url: CHANNEL_LINK }) }
            ]
        }, msg);
        await react('✅');
    }
});

commands.push({
    name: 'info',
    description: 'Show bot information',
    aliases: ['bot'],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        const now = moment().tz(config.TIMEZONE || 'Africa/Nairobi');
        const timeStr = now.format('h:mm A');
        const dateStr = now.format('DD/MM/YYYY');
        
        const uptime = process.uptime();
        const days = Math.floor(uptime / 86400);
        const hours = Math.floor((uptime % 86400) / 3600);
        
        const totalMem = os.totalmem() / (1024 * 1024 * 1024);
        const freeMem = os.freemem() / (1024 * 1024 * 1024);
        const usedMem = (totalMem - freeMem).toFixed(2);
        const totalMemFixed = totalMem.toFixed(2);

        const infoText = `📱 *BOT INFO*\n\n` +
            `👤 *Owner:* ${config.OWNER_NAME}\n` +
            `📞 *Phone:* ${config.OWNER_NUMBER}\n` +
            `🤖 *Bot:* ${config.BOT_NAME}\n` +
            `🔧 *Prefix:* ${config.PREFIX}\n` +
            `⚙️ *Mode:* ${config.MODE}\n` +
            `📚 *Commands:* ${bot.commands.size}\n` +
            `⏱️ *Uptime:* ${days}d ${hours}h\n` +
            `🕒 *Time:* ${timeStr}\n` +
            `📅 *Date:* ${dateStr}\n` +
            `💾 *RAM:* ${usedMem}GB/${totalMemFixed}GB`;

        await sendButtonMenu(sock, from, {
            title: '📱 Bot Information',
            text: infoText,
            footer: '> created by wanga',
            image: BOT_LOGO,
            buttons: [
                { id: `${config.PREFIX}status`, text: '📊 Status' },
                { id: `${config.PREFIX}menu`, text: '📋 Menu' },
                { name: 'cta_url', buttonParamsJson: JSON.stringify({ display_text: '📢 Channel', url: CHANNEL_LINK }) }
            ]
        }, msg);
        await react('✅');
    }
});

commands.push({
    name: 'status',
    description: 'Show bot status',
    aliases: ['stats'],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        const uptime = process.uptime();
        const days = Math.floor(uptime / 86400);
        const hours = Math.floor((uptime % 86400) / 3600);
        const minutes = Math.floor((uptime % 3600) / 60);
        const seconds = Math.floor(uptime % 60);
        
        const memory = process.memoryUsage();
        const usedMB = Math.round(memory.heapUsed / 1024 / 1024);
        const totalMB = Math.round(os.totalmem() / 1024 / 1024);

        const statusText = `📊 *BOT STATUS*\n\n` +
            `⏱️ *Uptime:* ${days}d ${hours}h ${minutes}m ${seconds}s\n` +
            `💾 *Memory:* ${usedMB}MB / ${totalMB}MB\n` +
            `📚 *Commands:* ${bot.commands.size}\n` +
            `⚡ *Node:* ${process.version}\n` +
            `💻 *Platform:* ${process.platform}`;

        await sendButtonMenu(sock, from, {
            title: '📊 Bot Status',
            text: statusText,
            footer: '> created by wanga',
            buttons: [
                { id: `${config.PREFIX}info`, text: 'ℹ️ Info' },
                { id: `${config.PREFIX}menu`, text: '📋 Menu' },
                { name: 'cta_url', buttonParamsJson: JSON.stringify({ display_text: '📢 Channel', url: CHANNEL_LINK }) }
            ]
        }, msg);
        await react('✅');
    }
});

commands.push({
    name: 'owner',
    description: 'Show owner information',
    aliases: ['creator'],
    async execute({ msg, from, sender, bot, sock, react, reply, buttons }) {
        const ownerInfo = `👑 *OWNER INFO*\n\n` +
            `📛 *Name:* ${config.OWNER_NAME}\n` +
            `📞 *Phone:* ${config.OWNER_NUMBER}\n` +
            `🌍 *Country:* Kenya\n\n` +
            `> created by wanga`;

        await sendButtonMenu(sock, from, {
            title: '👑 Bot Owner',
            text: ownerInfo,
            footer: '> created by wanga',
            buttons: [
                { id: `${config.PREFIX}info`, text: 'ℹ️ Info' },
                { id: `${config.PREFIX}menu`, text: '📋 Menu' },
                { name: 'cta_url', buttonParamsJson: JSON.stringify({ display_text: '📢 Channel', url: CHANNEL_LINK }) }
            ]
        }, msg);
        await react('✅');
    }
});

module.exports = { commands };
